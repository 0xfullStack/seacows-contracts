{
  "name": "@yolominds/seacows-contracts",
  "version": "1.1.1",
  "private": "true",
  "license": "MIT",
  "scripts": {
    "release": "npx standard-version --message 'chore(release): %s\n\n[skip ci]' --no-verify"
  },
  "workspaces": [
    "packages/*"
  ]
}
